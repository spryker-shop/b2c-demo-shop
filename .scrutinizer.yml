build:
    environment:
        php: '8.1.0'
        node: '14.0'

    tests:
        override:
            - true # disable test execution on scrutinizer

    nodes:
        analysis:
            requires:
                - branch: /^release-.+$/
            tests:
                override:
                    - php-scrutinizer-run
