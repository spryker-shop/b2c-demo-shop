build:
    environment:
        php: '8.0.19'
        docker: true

    tests:
        override:
            - true # disable test execution on scrutinizer

    nodes:
        analysis:
            tests:
                override:
                    - php-scrutinizer-run
                    - docker run -v $PWD:/src -w /src node:18 js-scrutinizer-run
