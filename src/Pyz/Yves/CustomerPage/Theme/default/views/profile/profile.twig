{% extends template('page-layout-customer', 'CustomerPage') %}

{% define data = {
    forms: {
        profile: _view.profileForm,
        password: _view.passwordForm
    },

    title: 'customer.account.profile_data' | trans,
    activePage: 'profile'
} %}
{% block customerContent %}
    <div class="grid grid--justify">
        <div class="col--lg-6 col--sm-12">
            {% include molecule('customer-form', 'CustomerPage') with {
                class: 'box',
                data: {
                    title: 'customer.profile.update' | trans,
                    form: data.forms.profile,
                    layout: {
                        salutation: 'col col--sm-12 col--lg-2',
                        first_name: 'col col--sm-12 col--lg-5',
                        last_name: 'col col--sm-12 col--lg-5'
                    },
                    submit: {
                        enable: true
                    }
                }
            } only %}
        </div>
        <div class="col--lg-6 col--sm-12">
            {% include molecule('customer-form', 'CustomerPage') with {
                class: 'box',
                data: {
                    title: 'customer.password.update' | trans,
                    form: data.forms.password,
                    submit: {
                        enable: true
                    }
                }
            } only %}
        </div>
        <div class="col--lg-6 col--sm-12">
            <div class="box">
                <div class="customer-account__box grid grid--justify grid--middle box">
                    <h5 class="customer-account__subtitle col">{{ "customer.profile.delete" | trans }}</h5>
                </div>
                <p>{{ "customer.profile.delete.description" | trans }}</p>
                <a href="{{url('customer/delete')}}" class="button button--white">{{ "customer.profile.delete" | trans }}</a>
            </div>
        </div>
    </div>



{% endblock %}
