{% extends template('widget') %}

{% define data = {
    nodes: _widget.navigationTree.nodes
} %}

{% block body %}
    {% include molecule('navigation-header') with {
        class: 'navigation-header grid grid--middle grid--center',
        data: {
            nodes: data.nodes,
            menuInline: false,
            menuDropdown: true
        }
    } only %}
{% endblock %}
