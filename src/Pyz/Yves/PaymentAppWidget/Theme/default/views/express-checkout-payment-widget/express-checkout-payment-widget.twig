{% extends template('widget') %}

{# B2C has no express checkout payment methods (no ExpressCheckoutPaymentWidgetRenderStrategyPlugins registered), so the widget content endpoint always returns empty content. Skip the fetch-on-load ajax scaffolding to avoid a useless request on every cart render. #}
{% block body %}{% endblock %}
