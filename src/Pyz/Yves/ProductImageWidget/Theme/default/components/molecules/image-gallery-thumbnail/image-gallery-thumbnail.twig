{% extends model('component') %}

{% define config = {
    name: 'image-gallery-thumbnail'
} %}

{% define data = {
    images: []
} %}

{% block body %}
    {% for i, image in data.images %}
        <div class="{{ config.name }}__item {% if i == 0 %}{{config.name}}__item--active{% endif %} {{ config.jsName }}__item"  data-thumbnail-index="{{ i }}">
            {% if image.externalUrlLarge is not empty %}
                <img class="{{ config.name }}__img" src="{{ image.externalUrlLarge }}" alt="{{ 'page.product.details.slider_thumbnail' | trans }}">
            {% endif %}
        </div>
    {% endfor %}
{% endblock %}
