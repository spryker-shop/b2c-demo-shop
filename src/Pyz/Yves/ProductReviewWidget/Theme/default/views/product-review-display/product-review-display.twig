{% extends view('product-review-display', '@SprykerShop:ProductReviewWidget') %}

{% block body %}
    {% include molecule('rating-selector', 'ProductReviewWidget') with {
        modifiers: ['small'],
        data: {
            value: data.value,
            maxValue: data.maxValue,
            useHalfSteps: true
        },
        attributes: {
            readonly: true
        }
    } only %}
{% endblock %}
