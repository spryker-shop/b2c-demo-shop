{% extends molecule('ordered-configured-bundle-quantity', '@SprykerShop:SalesConfigurableBundleWidget') %}

{% block quantity %}
    <span>
        {{ 'configured_bundle_widget.multiplier' | trans }} {{ data.quantity | executeFilterIfExists('formatInt', app.locale) }}
    </span>
{% endblock %}
