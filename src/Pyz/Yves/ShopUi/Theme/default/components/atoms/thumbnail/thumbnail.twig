{% extends atom('thumbnail', '@SprykerShop:ShopUi') %}

{% block image %}
    <img class="{{ component.renderClass(config.name ~ '__image', modifiers) }} {{ additionalImageClass }}" {{ component.renderAttributes(attributes) }}>
{% endblock %}
