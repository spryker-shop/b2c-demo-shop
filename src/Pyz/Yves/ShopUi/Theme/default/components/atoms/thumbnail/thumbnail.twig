{% extends model('component') %}
 
{% define config = {
    name: 'thumbnail',
    tag: 'span',
    renderAttributes: false
} %}

{% block body %}
    <img class="{{config.name}}__image" {{block('attributes')}} />
{% endblock %}
