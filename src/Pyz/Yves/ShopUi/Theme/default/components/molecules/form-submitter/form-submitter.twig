{% extends model('component') %}

{% define config = {
    name: 'form-submitter',
    tag: 'form-submitter',
} %}

{% define attributes = {
    'trigger-class-name': required,
    'event': 'change',
} %}

{%- block class -%}
    {{ parent() }} {{ config.jsName }}
{%- endblock -%}
