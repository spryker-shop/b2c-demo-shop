{% extends model('component') %}

{% define config = {
    name: 'nav-overlay-toggler',
    tag: 'nav-overlay-toggler'
} %}

{% define attributes = {
    'trigger-open': required,
    'trigger-close': required,
    'blocks': required,
    'toggle-target': required,
    'active-link': required,
    'class-to-toggle': 'is-active'
} %}
