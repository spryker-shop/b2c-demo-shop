{% extends model('component') %}

{% define config = {
    name: 'toggler-accordion',
    tag: 'toggler-accordion'
} %}

{% define attributes = {
    trigger: required,
    'class-to-toggle': 'is-hidden',
    activeClass: 'active'
} %}


