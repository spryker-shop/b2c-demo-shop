{% extends template('page-layout-customer', 'CustomerPage') %}

{% define data = {
    wishlists: required,
    title: 'customer.account.wishlist' | trans,
    activePage: 'wishlist',
    activeWishlistId: null
} %}

{% block customerNavigation %}
    {{ widgetGlobal('CustomerNavigationWidgetPlugin', data.activePage, data.activeWishlistId) }}
{% endblock %}
