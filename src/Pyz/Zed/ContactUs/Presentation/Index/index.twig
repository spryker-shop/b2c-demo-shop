{% extends '@Gui/Layout/layout.twig' %}

{% block content %}
    <h3>Messages</h3>
    {% for data in contactUsData %}
        <div>
            <h4>Name: {{ data.name }}</h4>
            <div>Message: {{ data.message }}</div>
        </div>
    {% endfor %}
{% endblock %}
